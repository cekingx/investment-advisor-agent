services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: investment-advisor-agent
    restart: unless-stopped
    env_file: .env
    ports:
      - "8081:3000"
    networks:
      - dev-net 

networks:
  dev-net:
    external: true
